import { ImagePlaceholderPipe } from './image-placeholder.pipe';

describe('ImagePlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
