import {AuthorisationDirective} from './authorisation.directive';

describe('AuthorisationDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorisationDirective();
    expect(directive).toBeTruthy();
  });
});
